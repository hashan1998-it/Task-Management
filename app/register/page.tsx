import { containerStyle } from "../constants/styles";
function Register() {
    return (
        <main className={containerStyle}>
            Register
        </main>
    );
}

export default Register;
